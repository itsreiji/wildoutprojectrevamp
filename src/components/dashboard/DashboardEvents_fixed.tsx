// ... [Previous imports and component code remain the same until the end of the file]

                    />
                </DialogContent>
            </Dialog>
        </div>
    );
};

// Set display name
DashboardEvents.displayName = 'DashboardEvents';

export default DashboardEvents;
